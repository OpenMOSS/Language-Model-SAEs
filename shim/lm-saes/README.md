# lm-saes

**This package has been renamed to [llamascopium](https://pypi.org/project/llamascopium/).**

Install the new package:

    pip install llamascopium

This shim package depends on `llamascopium` and re-exports everything for backward compatibility.
